import * as vector from './vector'
import * as vectors from './vectors'

export const {
  angle,
  clamp,
  conjugate,
  divide,
  fromAngle,
  length,
  magnitude,
  mod,
  multiply,
  normalize,
  remainder,
  rotate,
  setLength,
  setMagnitude,
  shift,
  times,
  toCartesian,
  toCylindrical,
  toPolar,
} = vector

export const {
  add,
  crossProduct,
  dotProduct,
  scalarProduct,
  subtract,
  sum,
  vectorProduct,
} = vectors
