import * as vector from './vector'
import * as vectors from './vectors'

export const {
  ZERO_VECTOR,
  angle,
  clamp,
  conjugate,
  divide,
  fromAngle,
  length,
  magnitude,
  mod,
  multiply,
  normalize,
  remainder,
  rotate,
  setAngle,
  setLength,
  setMagnitude,
  shift,
  times,
  toCartesian,
  toCylindrical,
  toPolar,
  unit,
} = vector

export const {
  add,
  crossProduct,
  dotProduct,
  scalarProduct,
  subtract,
  sum,
  vectorProduct,
} = vectors
