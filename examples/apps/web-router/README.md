# lit-router

Demonstrates the client-side, entity-based router included with `@inglorious/web` and how to wire routes to entity types.

Quick start:

```bash
cd examples/apps/web-router
pnpm install
pnpm dev
```

Files of interest:

- `src/store` — wiring the `router` type into your store
- `src/*` — example pages and route handling
